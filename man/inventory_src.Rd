% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inventory-src.R
\name{inventory_src}
\alias{inventory_src}
\title{Inventory a source data list}
\usage{
inventory_src(.src_list, .subject_col = "USUBJID")
}
\arguments{
\item{.src_list}{A named list of source data frames, typically returned by
\code{\link[=read_src_dir]{read_src_dir()}}. Elements whose names start with \code{mrgda_} are treated as
package metadata and excluded from the domain inventory.}

\item{.subject_col}{A single character string naming the subject identifier.
Defaults to \code{"USUBJID"}.}
}
\value{
An object of class \code{mrgda_src_inventory}. This is a list containing:
\describe{
\item{domains}{One row per source domain with dimensions, inferred source
variables, and key-integrity counts.}
\item{variables}{One row per source variable with its label, class, inferred
role, missing count, and distinct-value count.}
}
}
\description{
Create a compact, deterministic inventory of the clinical source datasets
returned by \code{\link[=read_src_dir]{read_src_dir()}}. The inventory summarizes domain dimensions,
identifiers, likely sequence variables, date/time and result variables, key
integrity, variable classes, labels, missingness, and cardinality.

\code{inventory_src()} reports source structure; it does not validate scientific
plausibility or modify source data.
}
\examples{
path <- system.file("example-sdtm", package = "mrgda")
src <- read_src_dir(path, .file_types = "xpt")
inventory_src(src)

}
