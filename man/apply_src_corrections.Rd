% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply-src-corrections.R
\name{apply_src_corrections}
\alias{apply_src_corrections}
\title{Apply and document confirmed source-data corrections}
\usage{
apply_src_corrections(
  .src_list,
  .corrections = NULL,
  .report_file,
  .print = c("changed", "record", "none"),
  .context = 2L
)
}
\arguments{
\item{.src_list}{A named list of source data frames, usually a direct call to
\code{\link[=read_src_dir]{read_src_dir()}}.}

\item{.corrections}{A data frame with one row per corrected source record and
columns \code{domain}, \code{USUBJID}, \code{seq_variable}, \code{seq_value}, \code{variable},
\code{original_value}, \code{corrected_value}, and \code{reason}. Values are represented
as character data so corrections to mixed source types can be row-bound.
\code{seq_variable} and \code{seq_value} normally hold the domain sequence key; use
another single-record source key when the domain has no sequence variable.
Use \code{NULL} or a zero-row data frame when no corrections are required.}

\item{.report_file}{Path to the Markdown correction report.}

\item{.print}{Console display mode. \code{"changed"} prints a focused before/after
comparison plus focused context, \code{"record"} prints complete source rows,
and \code{"none"} suppresses correction output.}

\item{.context}{Number of neighboring source rows to print on each side of
the corrected row. Context is limited to the same \code{USUBJID} and retains
source order. Focused output includes source identifiers, timing and visit
variables, and the corrected variable. Set to \code{0} to suppress the context
tables.}
}
\value{
The source list with corrections applied and a typed \code{corrections}
tibble added.
}
\description{
Apply a declarative table of confirmed corrections to a source list, append
executable audit data as \code{corrections}, and regenerate a Markdown correction
report. Every correction is preflighted before any source value is changed.
Corrections are applied, printed, and logged in canonical source-key order.

The incomplete-run notice is written before \code{.src_list} is evaluated. Pass
\code{\link[=read_src_dir]{read_src_dir()}} directly as \code{.src_list} so a failed source read cannot leave
a stale successful report.
}
\examples{
path <- system.file("example-sdtm", package = "mrgda")
report <- tempfile(fileext = ".md")

corrections <- tibble::tribble(
  ~domain, ~USUBJID, ~seq_variable, ~seq_value, ~variable,
  ~original_value, ~corrected_value, ~reason,
  "dm", "CDISC01.100008", NA, NA, "AGE",
  "72", "73", "Confirmed against the source query"
)

src <- apply_src_corrections(
  .src_list = read_src_dir(path, .file_types = "xpt", .read_domains = "dm"),
  .corrections = corrections,
  .report_file = report
)

}
